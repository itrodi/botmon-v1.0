# Botmon frontend environment variables
#
# Copy this file to `.env.local` (git-ignored) and override values as needed.
# All values must be prefixed with VITE_ to be exposed to the client.

# Base URL of the Botmon backend API.
# Defaults to https://api.automation365.io when unset.
VITE_API_URL=https://api.automation365.io
